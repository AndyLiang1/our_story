import * as React from 'react';

export interface ISideBarProps {
}

export function SideBar (props: ISideBarProps) {
  return (
    <div className = "w-[15%] h-full bg-blue-50">
      Sidebar
    </div>
  );
}
