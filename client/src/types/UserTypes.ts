export type LoginType = {
    email: string, 
    password: string
}

export type LoginBEType = {
    firstName: string, 
    lastName: string, 
    authToken: string, 
    tiptapToken: string
}